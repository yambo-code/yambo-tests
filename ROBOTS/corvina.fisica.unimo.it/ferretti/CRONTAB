# Edit this file to introduce tasks to be run by cron.
# 
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
# 
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').# 
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
# 
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
# 
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
# 
# For more information see the manual pages of crontab(5) and cron(8)
#
#*     *     *   *    *        command to be executed
#-     -     -   -    -
#|     |     |   |    |
#|     |     |   |    +----- day of week (0 - 6) (Sunday=0)
#|     |     |   +------- month (1 - 12)
#|     |     +--------- day of        month (1 - 31)
#|     +----------- hour (0 - 23)
#+------------- min (0 - 59)
# 
# m  h   dom mon dow   command
#
50 19 * * * cd /scratch/ferretti/yambo-tests-robots/; ./configure 2>&1
00 20 * * * cd /scratch/ferretti/yambo-tests-robots/; ./driver.pl -kill 2>&1
10 20 * * * cd /scratch/ferretti/yambo-tests-robots/; ./driver.pl -c 2>&1
30 20 * * * cd /scratch/ferretti/yambo-tests-robots/; ./driver.pl -d all -force 2>&1
00 21 * * * cd /scratch/ferretti/yambo-tests-robots/; ./ROBOTS/corvina.fisica.unimo.it/ferretti/SCRIPTS/launcher.sh pgi validate_PGI_CUDA bug-fixes
00 01 * * * cd /scratch/ferretti/yambo-tests-robots/; ./ROBOTS/corvina.fisica.unimo.it/ferretti/SCRIPTS/launcher.sh gnu validate_GNU devel-SLK
00 05 * * * cd /scratch/ferretti/yambo-tests-robots/; ./ROBOTS/corvina.fisica.unimo.it/ferretti/SCRIPTS/launcher.sh pgi validate_PGI_CUDA devel-SLK
00 07 * * * cd /scratch/ferretti/yambo-tests-robots/; ./driver.pl -kill 2>&1
